import { StyleSheet } from "aphrodite";

const styles = StyleSheet.create({
  resultsContainer: {
    maxWidth: 900,
    margin: "auto",
    height: "100%",
    minHeight: "100vh",
  },
});

export default styles;
