import { StyleSheet } from "aphrodite";

const styles = StyleSheet.create({
  seekerContainer: {
    maxWidth: 900,
    margin: "auto",
    height: "100%",
    minHeight: "100vh",
  },
});

export default styles;
