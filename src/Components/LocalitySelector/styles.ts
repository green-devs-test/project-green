import { StyleSheet } from "aphrodite";

const styles = StyleSheet.create({
  newSearchDropdown: {
    fontWeight: 700,
    lineHeight: '20px',
    borderRadius: "8px",
    margin: "10px",
    padding: "15px",
    minWidth: "95%",
  },
  select: {
    borderRadius: "8px",
    textAlign: "center",
  },
  options: {
    borderRadius: "8px",
  },
});

export default styles;
